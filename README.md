# inverted-search-c
An efficient inverted search engine that indexes words from text files using a hash table and linked list. Provides fast and memory-efficient keyword lookups across large datasets.
